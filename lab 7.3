#include <stdio.h>
#include <math.h>

int main()
{
	float x, a, y;
	scanf_s(" %f %f %f", &a, &x, &y);
	a = a / x; // стоимость 1 кг конфет
	y = a * y; // стоимость н кг конфет
	printf(" %.0f %.0f", a, y);
	return 0;
}
