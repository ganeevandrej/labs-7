#include <stdio.h>
#include <math.h>

int main()
{
	int  a1, a2, b1, b2, c1, c2;
	float x, y, det;
	scanf_s("%d %d %d %d %d %d", &a1, &a2, &b1, &b2, &c1, &c2);
	det = a1 * b2 - a2 * b1;
	x = (c1 * b2 - c2 * b1) / det;
	y = (a1 * c2 - a2 * c1) / det;
	printf("%.1f %.1f", x, y);
	return 0;
}
